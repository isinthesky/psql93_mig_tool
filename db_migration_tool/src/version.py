"""애플리케이션 버전 — 앱이 실행 중에 읽는 단일 지점.

이 파일은 `tools/bump_version.py`가 갱신한다. 손으로 고치지 마라.
버전은 pyproject.toml · installer/DBMigrationTool.iss · 이 파일 세 곳에
있어야 하는데(각각 패키징·인스톨러·실행 중 표시), 따로 고치면 반드시
어긋난다 — 인스톨러 파일명은 1.2.1인데 앱 정보 창은 1.0.0을 말하는
상태가 실제로 있었다.
"""

__version__ = "1.2.1"
